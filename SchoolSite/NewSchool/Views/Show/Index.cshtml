@using NewSchool.Models
@model IEnumerable<NewSchool.Models.ShowModel>

@{
    ViewBag.Title = "师生风采";
}
@Html.Partial("_ScrollImage")
<h2>@ViewBag.Title</h2>
@if (Request.IsAuthenticated)
{
 <p>
    @Html.ActionLink("添加师生风采", "Create", null, new { @class = "btn btn-primary" })
</p>   
}


    @if (Model != null && Model.Any())
{
    foreach (ShowModel researchModel in Model)
    {
        <div class="panel col-md-6 panel-info" style="padding: 5px;">
            <div class="panel-heading">
                <div class="panel-title">
                    @researchModel.Title
                    @if (Request.IsAuthenticated)
                    {
                         @Html.ActionLink("修改", "Edit", new { id = researchModel.Id })
                @Html.ActionLink("删除", "Delete", new { id = researchModel.Id }) 
                    }
              
            </div>
        </div>
        <div class="panel-body">
            @Html.Raw(researchModel.Content)
        </div>
    </div>
    }
}

